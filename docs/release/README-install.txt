zv release archive

This archive contains the zv binary and supporting release metadata.

Quick install:
  curl -fsSL https://raw.githubusercontent.com/nburrus/zv/main/scripts/install.sh | bash

Manual install:
  1. Extract this archive
  2. Copy zv to ~/.local/bin/zv
  3. chmod 755 ~/.local/bin/zv
